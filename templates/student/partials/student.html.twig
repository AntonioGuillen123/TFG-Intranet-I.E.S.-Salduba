{% for student in students %}
	<div id="student-{{student.id}}" class="flex flex-col rounded-lg border-4 bg-white text-surface shadow-secondary-1 dark:bg-surface-dark dark:text-black md:min-w-96 md:flex-row">
		<img class="h-96 w-full rounded-t-lg object-cover md:h-auto md:w-48 md:!rounded-none md:!rounded-s-lg" src="{{student.image}}" alt="Imagen no encontrada :("/>
		<div class="flex flex-col justify-between p-6 md:min-w-96">
			<div class="w-100">
				<h5 class="mb-4 text-xl font-medium">{{student.student}}</h5>
				<p class="mb-2 text-sm">
					<b>
						DNI:
					</b>
					{{student.dniStudent}}
				</p>
				<p class="mb-2 text-sm">
					<b>
						Teléfono:
					</b>
					{{student.phone}}
				</p>
				<p class="mb-2 text-sm">
					<b>
						Email:
					</b>
					{{student.studentPersonalEmail}}
				</p>
				<p class="mb-2 text-sm">
					<b>
						Dirección:
					</b>
					{{student.address}}
				</p>
			</div>
			<div class="flex justify-between w-100">
				<span class="text-s text-surface/75 dark:text-neutral-300">
					<b>
						Nacimiento:
					</b>
					{{student.birthDay|date('d/m/Y')}}
				</span>
				<span class="text-s text-surface/75 dark:text-neutral-300">
					<span class="w-full px-3 py-1 text-sm text-white bg-cyan-700 rounded-full">
						{{student.unit}}
					</span>
				</span>
			</div>
		</div>
	</div>
{% endfor %}
