{% extends 'news/index.html.twig' %}

{% block titlePage %}
	Ausencias - Anotar Nueva
{% endblock %}

{% block toolbarPage %}
{% endblock %}

{% block pageContent %}
	{{ form_start(form) }}
		<div id="form" class="d-flex flex-column">

			<div class="form-group">
				{{ form_label(form.absence_date) }}
				{{ form_widget(form.absence_date) }}
			</div>

			<div class="form-group">
				{{ form_label(form.hour) }}
				{{ form_widget(form.hour) }}
			</div>

			<div class="form-group inline-form">
				{{ form_label(form.task) }}
				{{ form_widget(form.task) }}
			</div>

			<div class="form-group inline-form">
				{{ form_label(form.reason) }}
				{{ form_widget(form.reason) }}
			</div>

			<br>
			<br>

			<button type="submit" class="btn btn-primary">Anotar</button>
		<div>
	{{ form_end(form) }}
{% endblock %}
